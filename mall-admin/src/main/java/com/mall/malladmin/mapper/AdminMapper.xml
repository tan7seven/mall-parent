<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.malladmin.mapper.AdminMapper">

    <sql id="Base_Column_List">
        user_id, login_id, password, nickname, phone, head_portrait, role, is_usable, add_date,
        update_date
    </sql>

    <select id="findByLoginId" resultType="com.mall.malladmin.vo.AdminVo">
        SELECT
        <include refid="Base_Column_List" />
        FROM mall_user m WHERE m.login_id = #{loginId}
    </select>
</mapper>